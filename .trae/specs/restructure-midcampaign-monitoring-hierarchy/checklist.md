# 验收清单

## 主章节与编号

- [x] 促中工作台主章节继续按 `01 当前波次 → 02 下一波次 → 03 实时监控与调优建议` 排列。
- [x] 03 外层只出现一次主编号 `03`。
- [x] 03 内部依次使用 `03.1 / 03.2 / 03.3 / 03.4`。
- [x] 03 内部不再出现独立的 `01 / 02 / 03` 视觉章节编号。
- [x] 子章节编号在视觉和语义上明确从属于主章节 03。

## 内容顺序

- [x] `03.1` 标题为“核心指标监控”。
- [x] `03.1` 展示券引导支付 GMV、用户核销率、核销预算和支付兑换比。
- [x] `03.1` 保留核心指标追问入口。
- [x] `03.2` 标题为“核销与实验分析”。
- [x] `03.2` 包含趋势图、AI 数据洞察、领取核销拆解和消费券实验。
- [x] `03.2` 保留“大促领券核销分析 / 消费券实验数据”Tab。
- [x] `03.3` 标题为“竞对动态监控”。
- [x] `03.3` 包含竞对年份筛选、竞对变化、关注变化、应对动作和反馈。
- [x] `03.4` 标题为“归因结论与调优提示”。
- [x] `03.4` 包含整体结论、关键矛盾、主要归因、建议动作及“结论 / 依据 / 下一步”。
- [x] 四个子章节严格按 `03.1 → 03.2 → 03.3 → 03.4` 排列。

## 标题去重与语义

- [x] “当前核心指标监控”不再与 `03.1` 形成两个同级标题。
- [x] “实时竞对信息”和“当前竞对变化”不再作为两个同级标题重复同一职责。
- [x] 外层主章节标题使用 H2。
- [x] `03.1—03.4` 子章节标题使用 H3。
- [x] 图表、卡片、Tab 面板和拆解区内部标题使用 H4 或等价非章节标签。
- [x] 每个章节的 `aria-labelledby` 指向唯一且存在的标题 ID。
- [x] 页面无标题跳级、重复 ID 或仅靠视觉字号表达层级的问题。

## 数据与交互

- [x] KPI 值、趋势数据、领取核销数据、实验结果、竞对数据和归因文案未因重排改变。
- [x] 趋势指标切换继续可用。
- [x] 分析 Tab 切换继续可用且 ARIA 状态同步。
- [x] 领取核销的人群、渠道、面额切换继续可用。
- [x] 实验动作和选中反馈继续可用。
- [x] 竞对年份、变化动作和关注变化继续可用。
- [x] 整体结论、核心指标、趋势、领取核销、实验、竞对和归因追问继续进入原数据上下文。
- [x] 03 不出现独立 AI 策略建议清单、采纳按钮或采纳横幅。
- [x] 03 的调优提示不复制 01 的完整建议和采纳流程。

## 上下文与空态

- [x] 切换活动、年份、波次后，四个子章节的编号与顺序保持不变。
- [x] 有数据上下文展示对应 KPI、趋势、实验、竞对和归因内容。
- [x] 缺数上下文保留 `03.1—03.4` 标题并显示现有空态或禁用状态。
- [x] 缺数时不隐藏章节导致跳号。
- [x] 缺数时不引用其他活动、年份或波次的数据。

## 响应式与可访问性

- [x] 桌面宽屏、1440px、1280px、1024px、860px 和 760px 下章节顺序一致。
- [x] 子章节编号不换行、截断或遮挡标题。
- [x] 标题、说明、追问和筛选控件不重叠。
- [x] 页面无整页横向溢出。
- [x] 宽表和图表只在各自容器内适配或滚动。
- [x] Tab、按钮、筛选器和追问支持键盘操作。
- [x] `aria-selected`、`aria-pressed`、`aria-controls`、`tabindex` 和焦点状态与视觉一致。
- [x] Axe 无新增 critical 或 serious 违规。

## 静态与回归

- [x] JavaScript 语法检查通过。
- [x] 静态与运行时重复 ID 检查通过。
- [x] 标题层级自动检查或结构化断言通过。
- [x] `git diff --check` 通过。
- [x] 浏览器控制台无新增页面错误。
- [x] 网络请求无新增异常。
- [x] 01 当前波次调优无回归。
- [x] 02 下一波次方案明细与调优无回归。
- [x] 促前默认工作台和既有促前/促中历史会话无回归。
