# 验收清单

## 入口与默认状态

- [x] 点击“促前券档位设计”进入独立促前工作台，不再只显示 Toast。
- [x] 促前顶部仅展示极简标题和必要的返回/移动端报告入口。
- [x] 首次进入仅展示目标重述、数据状态和七步分析计划。
- [x] 右侧七章报告仅第一章等待确认，其余章节锁定。
- [x] 返回工作台后原筛选和图表状态仍可用。

## 脱敏 Mock 数据

- [x] 页面明确标注“脱敏 Mock，仅用于原型演示”。
- [x] 活动、实验、站点和人群 ID 均为随机别名。
- [x] 竞对使用 A/B/C 别名，不出现真实平台名称。
- [x] 团队和审批人仅显示角色，不显示个人身份。
- [x] 金额、规模和日期无法反推真实业务数据。
- [x] 数据来源区分 ECOP Mock、历史复盘 Mock、竞对调研 Mock、用户输入和 AI 推导。

## 七步分析链路

- [x] 目标约束包含增长目标、ROI 下限、预算上限、CAC、人均补贴和治理要求。
- [x] 大盘水位展示近 20 天 GMV 93.6 亿、DAC 2,860 万、客单 327 及同比。
- [x] AI 指出 GMV +7.8% 主要由 DAC +7.1% 驱动，客单仅 +0.6%。
- [x] 类目章节展示活动放大系数、DAC/客单拉动、增量贡献和优先级。
- [x] 数码家电与食品生鲜为 S 级，并给出指标依据。
- [x] 支持一级类目下钻到二级类目和建议券档。
- [x] 历史核销支持人群、渠道和面额三类切换。
- [x] 面额漏斗展示发放、领取、核销、领取率、领取后核销率和 ROI。
- [x] AI 能区分低领取、高领取低核销、高核销低钱效和高钱效低规模。
- [x] 实验章节展示 3+X/4+X 券组人均 GMV、增量 GMV、补贴、ROI 和显著性。
- [x] 竞对章节展示竞对 A/B/C 档位、折扣、会员加码、发券频次和影响等级。
- [x] 支持类目 × 人群 × 面额 × 渠道交叉下钻。

## 方案和预算

- [x] 页面展示 A-3+X钱效优先、B-4+X均衡增长、C-4+X峰值增长。
- [x] 每套方案展示申请预算、结算率、结算预算、增量 GMV、ROI 和主要风险。
- [x] 推荐 B 方案，并解释其增量、ROI 和预算弹性平衡。
- [x] 固定档位包含 300-40、600-80、1500-180，4 档方案增加 3000-420。
- [x] X 券明确按类目 × 人群 × 渠道定向。
- [x] 预算按预热、第一波、第二波、收官、专项子预算和风险准备金拆分。
- [x] 支持保守/基准/激进结算率场景。
- [x] 支持预算缩减 10% 和需求超预期 15% 场景。
- [x] 展示策略、财务、业务、安全等角色确认状态。
- [x] 展示预算申请、活动立项、专项子预算和保密协议状态。

## AI 洞察与追问

- [x] 每个章节包含结论、证据、机制、限制和下一步。
- [x] 预设追问覆盖大盘水位、类目爆发、核销、实验、竞对、预算和治理。
- [x] 自由输入会展示理解、定位、计算和回答过程。
- [x] 追问答案引用当前章节与脱敏 Mock 指标。
- [x] 类目优先级追问引用 GMV 放大系数、DAC、客单和增量贡献。
- [x] 高门槛券追问能解释核销和钱效问题。
- [x] 竞对追问不会仅因竞对补贴更深就建议盲目跟随。

## 报告与交互

- [x] 报告章节按对话动作逐章生成，不直接跳到最终方案。
- [x] 报告 Queue 正确区分锁定、排队、生成中、完成和等待输入。
- [x] 单章生成过程约 2.6 秒，中间状态清晰可见。
- [x] 右侧报告支持自定义标题、隐藏章节和恢复章节。
- [x] 最终 HTML 报告与待审批策略包在中栏底部固定展示。
- [x] 报告支持查看、全屏、返回队列和下载。
- [x] 确认推荐方案只生成待审批状态，不触发真实 ECOP 操作。

## 视觉、可访问性与回归

- [x] 促前沿用深色导航、中部白色会话、右侧浅灰报告工作区。
- [x] 关键图表、表格、方案卡和预算场景具备清晰层级。
- [x] 所有按钮、输入框、Tab 和追问入口具备可读名称。
- [x] 键盘可操作主要流程，Escape 可关闭浮层/预览。
- [x] 桌面宽屏和 1280px 布局无横向溢出或遮挡。
- [x] JavaScript 语法检查通过。
- [x] 页面不存在重复 ID。
- [x] 浏览器控制台无新增错误。
- [x] “促中预算紧张”完整链路仍可进入和操作。
- [x] 原工作台筛选、图表、竞对和策略详情仍可用。

## GitHub 发布

- [x] 本地仓库默认分支为 `main`。
- [x] 提交内容不包含访问凭证、Token、真实 ECOP 查询参数或临时下载文件。
- [x] GitHub 远端为 `https://github.com/jiaoyunqin/ai--coupon--strategy`。
- [x] 推送过程未使用 `--force`，未覆盖未知远端历史。
- [x] 远端 `main` commit 与本地 HEAD 一致。
- [x] 远端仓库包含可直接打开的完整七步 `index.html` 和本次规格文档。

## 活动日历与参考促

- [x] 进入促前规划后，页面首先展示“26 年活动日历”下拉，不展示活动卡片墙。
- [x] 当前活动摘要展示活动名称、日期、级别、阶段和历史数据状态。
- [x] 页面不再展示“这个活动属于哪一类促”的促类型步骤。
- [x] 切换规划活动后，当前活动摘要和全部洞察同步更新。
- [x] 系统自动纳入所选活动前序最近四次促。
- [x] 系统自动纳入与所选活动同级别的历史促。
- [x] 系统自动纳入所选活动的同名历史促。
- [x] 自动参考促集合去重，并标注前序四次、同级别、同名历史等入选原因。
- [x] 用户可以删除自动参考促。
- [x] 用户可以从历史活动列表添加额外参考促。
- [x] 添加或删除参考促后，样本数量、覆盖范围和洞察同步更新。

## 大盘水位与类目爆发

- [x] 大盘水位展示活动前 15–20 天 GMV / DAC / 客单趋势。
- [x] 大盘水位展示与去年同期的同比水位和增长来源。
- [x] 类目洞察比较活动前 15–20 天与历史活动期的分类目表现。
- [x] 类目洞察至少展示 GMV、DAC、客单和活动放大情况。
- [x] 页面识别需求爆发类目，并说明证据与适用边界。

## 券档、波次与人群洞察

- [x] 券档洞察展示各档领取、核销、领后核销和钱效趋势。
- [x] 券档洞察支持行业下钻。
- [x] 券档洞察支持价格带下钻。
- [x] 波次洞察覆盖预热、爆发、持续、收官等历史阶段。
- [x] 波次洞察展示券档、券批次节奏和核销表现。
- [x] 人群洞察覆盖价值、潜力、新客、回流等分层。
- [x] 人群洞察展示不同分层的实验差异和样本限制。

## 竞对、来源与追问

- [x] 竞对聚合展示券档、折扣、张数、频次和变化方向。
- [x] 竞对信息明确标记为外部参考信号，不直接替代自身实验结论。
- [x] 洞察来源至少覆盖消费券数据看板和消费券实验框架。
- [x] 波次洞察引用批次及投放计划。
- [x] 渠道或竞对洞察引用渠道投放策略或竞对归档。
- [x] 每个洞察章节包含证据、AI 小结、适用边界和来源。
- [x] 点击追问后携带当前活动、参考促集合、洞察主题、证据和来源摘要。

## 信息边界、视觉与回归

- [x] 首页洞察文案明确是历史规律或参考，不表述为本期确定方案。
- [x] 首页不出现“推荐 B”“申请预算”“预估 ROI”等方案推荐内容。
- [x] 首页不直接推荐本期采用 3+X、4+X 或具体券档组合。
- [x] 首页不展示方案形成后的配置巡检模块。
- [x] 顶部活动选择和参考促区域轻量、紧凑，不使用大幅方案结论卡。
- [x] 活动、参考促和洞察区具有明确阅读顺序。
- [x] 桌面宽屏和 1280px 下无横向溢出或内容遮挡。
- [x] JavaScript 语法与 diff 检查通过。
- [x] 页面无新增静态重复 ID 和控制台错误。
- [x] 历史“促前券档位设计”对话仍可进入并保留七步流程。
- [x] 促中调优工作台及其历史对话不受影响。
- [x] 删除自动参考促后重新加入同一活动时，参考原因保留原自动命中关系并叠加“用户添加”，且相同样本集合恢复与删除前一致的证据指标和 AI 小结。

## 参考促时效权重与动态窗口

- [x] 参考促时效权重按规划活动与参考促的实际日期间隔连续衰减，跨自然年时不存在权重跳变。
- [x] 参考促窗口以当前规划活动日期为锚点动态覆盖向前 24 个月，窗口外活动和未来活动不参与自动参考计算。
- [x] 仅由用户添加的参考促采用中立时效权重，不因添加顺序或跨年额外增减权重。
- [x] 同时命中自动规则和用户添加的参考促保留自动原因及对应时效权重，“用户添加”仅作为来源标记。
- [x] 跨年、闰年、恰好 24 个月、边界前后 1 天、未来日期和用户添加边界测试全部通过。

## 工作台与飞书 Mock 回归

- [x] 新建对话后消息、报告、任务选择、定时任务选择和临时交互状态均恢复初始值，不继承上一对话内容。
- [x] `contextRange` 在重渲染、视图切换和返回对话后保持持久化，界面选值与实际上下文范围一致。
- [x] 任务明细数组、列表条目数、角标和汇总计数一致。
- [x] 定时任务明细数组、列表条目数、角标和汇总计数一致。
- [x] 任务或定时任务为零项时展示明确空态，不残留明细、错误计数或不可用操作。
- [x] 飞书机器人群名称、群 ID、成员和关联信息均为不可反推的脱敏 Mock，列表与详情字段一致。
- [x] 新建对话、`contextRange`、任务/定时任务、零项空态和飞书 Mock 回归验证通过，促前/促中原有链路无新增回归。

## 促前洞察展开与追问

- [x] 点击大盘水位或类目爆发的“展开证据”后显示详细表、历史证据和适用边界，再次点击可完整折叠。
- [x] 洞察展开与折叠时，按钮 `aria-expanded`、按钮文案和卡片展开状态保持同步。
- [x] 点击洞察“追问”后进入促前历史对话，输入框或用户消息包含规划活动、参考促集合、洞察标题、证据、来源和适用边界。
- [x] 浏览器回归确认券档行业/价格带下钻、促前七步历史对话和促中链路不受影响。

## 促前首页信息边界与追问前缀

- [x] 首页摘要明确标注“历史证据摘要 / 仅供本期分析参考”，只陈述当前参考样本中的自然水位、类目差异、券档和渠道历史表现。
- [x] 首页摘要不使用祈使句或本期执行动作，不出现推荐 B、申请预算或具体本期券档组合。
- [x] “进入策略共创”入口保留，带入对话的上下文标签和摘要明确为历史证据而非本期方案。
- [x] 洞察追问上下文中的 `AI 小结：` 仅出现一次，不因 `insight.summary` 已带前缀而重复。
- [x] 浏览器回归确认七类洞察、洞察追问和“进入策略共创”入口仍可用。
- [x] JavaScript 语法、静态 ID、目标文案和 diff 检查通过。

## 推荐方案优先首屏

- [x] “查看策略灵感”的促前规划和促中调优均先展示推荐方案，再展示原始数据与图表。
- [x] 促前规划保留“26 年活动日历”和参考促管理，不恢复促类型选择步骤。
- [x] 促前首屏候选策略灵感展示推荐方向、方案结构、证据置信度和关键预估。
- [x] 促前候选策略明确标注为待共创，不触发真实预算申请、配置或发布。
- [x] 促前趋势、类目机会、历史实验洞察及其切换、下钻、来源和追问能力仍可用。
- [x] 促中首屏同时展示“当前波次人群与灵活渠道调整”和“下一波次方案设计”。
- [x] 促中两个方案均有独立追问入口，追问上下文与对应方案一致。
- [x] 点击任一推荐方案可展开推荐结论、方案结构、推荐逻辑、风险与预估，再次点击可收起。
- [x] 方案展开状态、按钮文案和 `aria-expanded` 保持同步。
- [x] 每个方案详情固定展示“基于此方案继续调整”按钮。
- [x] 点击“基于此方案继续调整”会创建独立对话，并携带当前活动及该方案的结论、结构、逻辑、风险和预估。
- [x] 促中原有筛选器、指标、竞对、实验、领券核销和投放矩阵保留在推荐方案下方且交互可用。
- [x] 桌面宽屏和 1280px 下推荐区域无横向溢出、文字截断或按钮遮挡。
- [x] JavaScript 语法、静态 ID、目标文案和 `git diff --check` 检查通过。
- [x] “促前券档位设计”历史对话七步流程与确认门禁没有被修改。
- [x] “促中预算紧张”历史对话仍可进入和操作。
- [x] 促前 GMV / DAC / 客单 Tab 与趋势数据点可点击切换，图表、解释和 ARIA 状态同步更新。
- [x] 促前类目机会 / 券档效率 Tab 与排行条目可点击切换，动作解释和选中状态同步更新。
- [x] 促前历史洞察分类、行业 / 价格带下钻与洞察展开交互均可用。
- [x] 促前趋势数据点支持 Enter / Space 键盘操作。

## 促中推荐上下文一致性

- [x] 促中推荐结论、方案结构、推荐逻辑、风险与预估均与当前所选活动、年份和波次一致。
- [x] 切换活动、年份或波次后，促中推荐数据同步刷新，不沿用上一上下文的推荐内容。
- [x] 当前活动、年份和波次无推荐数据时展示明确降级状态，不混用其他上下文的数据。
- [x] 独立追问和“基于此方案继续调整”对话携带的活动、年份、波次及方案上下文与当前选择一致。

## 证据交互与窄屏回归

- [x] 促前趋势数据点具备清晰、可感知的悬停、聚焦与选中状态。
- [x] 促前类目条目具备清晰、可感知的悬停、聚焦与选中状态。
- [x] 促中推荐下方原有筛选器、指标、竞对、实验、领券核销和投放矩阵交互均可用。
- [x] 954px、1024px 和 1280px 宽度下无横向溢出、文字截断或按钮遮挡。

## 促中追问上下文隔离与筛选证据回归

- [x] 仅促中双推荐入口创建推荐上下文。
- [x] KPI、趋势、归因、竞对等旧证据入口保持数据追问，不被推荐上下文替换。
- [x] 促中证据与当前活动、年份和波次筛选上下文同步。
- [x] 当前筛选上下文无数据时禁用旧建议，不展示或带入过期建议。
- [x] 1307px 和 1280px 窄屏下竞对卡无横向溢出、文字截断或按钮遮挡。

## 促前完整券波次配置工作台

- [x] 活动对象包含结束日、促类型、默认候选、波次周期和主端/独立端能力，且标注活动日历静态快照来源。
- [x] 切换 S/A/B 级活动后，参考促、默认候选、波次和配置同步重算。
- [x] 同时展示 A-3+X、B-4+X、C-4+X，默认候选具有推荐标记。
- [x] 用户手动切换候选后，顶部摘要、预算、结算、增量 GMV、ROI、覆盖规模和配置同步更新。
- [x] 参考促增删只重算证据和置信度，不覆盖用户手动选择的候选。
- [x] 波次总览支持主端/独立端，并展示周期、预算、目标、券档、人群和场域。
- [x] 无独立端活动明确展示不适用并禁用，不生成虚构波次。
- [x] 券档与批次包含场域计划、批次计划、批次列表、活动列表四个子视图。
- [x] 批次字段覆盖脱敏 code、3+X/4+X、发放/使用窗口、限领频控、端、人群、场域、预算和目标。
- [x] 人群渠道矩阵展示主投、补充、催用、对照、停投和渠道预算动作。
- [x] 实验区展示历史实验、C/T1/T2 分流、增量 GMV/ROI 指标和护栏。
- [x] 巡检区展示基础信息、实验参数、增发追补、投放一致性和发布边界，失败项标记阻塞。
- [x] 来源条覆盖活动日历、策略文档、渠道、批次、数据看板、实验和巡检站点。
- [x] 独立追问和“基于此方案继续调整”携带完整活动与方案配置上下文。
- [x] 页面统一标注脱敏 Mock、静态快照、待共创和不触发真实配置。
- [x] 历史证据层仍位于完整方案之后并保持全部切换、下钻和追问能力。
- [x] 促前历史对话确认门禁和促中工作台无回归。
- [x] JavaScript、静态 ID、`git diff --check`、控制台和 1440/1024/760px 布局检查通过。

## 浏览器控制台收尾

- [x] cache-bust 页面不再请求缺失 favicon.ico，控制台无新增错误。

## 促中纵向双模块与方案内容

- [x] 01 当前波次与 02 下一波次为两个独立全宽模块，并按顺序上下排列。
- [x] 当前波次模块展示诊断、目标、券档动作、人群切量、渠道迁移、分时节奏、关键指标、风险和止损线。
- [x] 当前波次的人群、渠道、券档、时点切换会同步更新证据和方案结论。
- [x] 当前波次每项动作均展示调整前、调整后、依据和预估影响。
- [x] 当前波次动作至少细化到“人群 × 券角色/档位 × 渠道/场域 × 端口 × 时间窗口”。
- [x] 当前波次支持放量、缩量、换档、迁移、催用、停投、保留对照和回滚动作。
- [x] 下一波次至少提供均衡补量、钱效优先和增长优先三套候选。
- [x] 每套下一波次候选均包含预算、券档、批次、窗口、限领频控、端口、人群、场域、实验、指标、风险和止损线。
- [x] 均衡补量、钱效优先和增长优先具有不同的预算动作、覆盖策略、准备金规则、ROI/核销护栏和适用条件。
- [x] 三套候选使用同一口径比较净增预算、覆盖规模、发放/领取/核销、结算率、支付/增量 GMV、ROI/兑换比、客单和风险。
- [x] 下一波次推荐候选有明确标记，其他候选仍可查看、追问和继续调整。
- [x] 原始趋势、竞对、实验、领券核销和投放数据保留在两个方案模块之后。

## 券方案配置与指标口径

- [x] 完整方案包含活动、年份、波次、日期和分天/分小时动作时点。
- [x] 完整方案区分第一张/第二张、普惠底座、定向增量、储备券、领前增发和催用任务。
- [x] 券批次包含券类型、门槛-面额、脱敏批次/券池别名、发放形式、发放窗口和使用窗口。
- [x] 人群规则包含八大人群、L1-L5、已领未用/高潜回流、算法比例和排除规则。
- [x] 渠道场域包含会场、搜索、券包、Push、直播/内容场、主端/独立端及主投/补充/催用/对照/停投角色。
- [x] 约束包含总预算、批次预算、库存、可腾挪预算、准备金、限领、频控、互斥和重复触达去重。
- [x] 指标按目标大盘、领取核销、预算结算、钱效业务、节奏流速、渠道场域、人群结构和实验护栏分层展示。
- [x] 目标大盘指标包含目标达成率、DAC、客单、大盘增量 GMV 和券增量占比。
- [x] 预算钱效指标包含结算金额/结算率、剩余可腾挪预算、支付/增量兑换比、ROI 和人均成本。
- [x] 场域与节奏指标包含分天/分小时流速、库存消耗、曝光/点击 UV/PV 和 CTR。

## 入口内容与历史会话兼容

- [x] 现有独立追问和“基于此方案继续调整”入口保持原交互结构。
- [x] 入口带入活动、年份、波次、当前/下一波次候选、完整券配置、指标摘要、风险和来源摘要。
- [x] 当前波次、均衡补量、钱效优先和增长优先四个入口的内容彼此隔离。
- [x] 入口不会跳转到固定的其他历史案例，也不会混入另一个候选内容。
- [x] 左侧历史会话导航和选中逻辑保持不变。
- [x] 历史会话整体布局、分析计划确认门禁和右侧报告结构保持不变。
- [x] 历史会话仅允许更新本次方案相关文案或摘要，不新增会话页面、不重排栏位。
- [x] KPI、趋势、归因、竞对等原始数据入口继续保持数据追问语义。
- [x] 带入内容不包含内部 URL、真实 token、真实批次 ID 或可反推业务明细。
- [x] 所有方案内容均为脱敏 Mock，不触发真实发券、预算或配置。

## 促中工作台回归

- [x] 1440px、1280px 和 1024px 下纵向方案模块无横向溢出、文字截断或按钮遮挡。
- [x] 维度切换、候选切换、独立追问和继续调整支持键盘操作并同步 ARIA 状态。
- [x] 促中原始数据证据入口和模块下方的数据交互仍可用。
- [x] “促中预算紧张”等历史会话的导航、布局、门禁和报告结构未被本次改动修改。
- [x] 促前默认工作台和“促前券档位设计”历史会话未被本次改动修改。
- [x] JavaScript 语法、静态 ID、控制台和 `git diff --check` 全部通过。

## 促前历史对话分析与三栏精修

- [x] 点击“促前券档位设计”进入左侧历史导航、中栏多轮对话、右侧七章 Queue/报告组成的独立三栏工作台。
- [x] 首次进入仅展示目标重述、已知与缺失数据、脱敏 Mock 声明和七步分析计划，不提前展示最终方案。
- [x] 用户确认分析计划后仅生成 P0 大盘水位，P1、核销、实验竞对、方案和预算治理按对话动作逐章解锁。
- [x] P0 展示活动前 15–20 天 GMV、DAC、客单、同比趋势和增长贡献拆分，并说明自然水位对券策略的影响。
- [x] P1 比较活动前窗口与历史活动期的类目 GMV、DAC、客单、放大系数和增量贡献，支持一级到二级类目下钻。
- [x] 历史领券核销按人群、渠道和面额展示发放、领取、核销、领取率、领后核销率、预算与 ROI，并解释不同低效类型。
- [x] 实验竞对章节展示实验显著性、增量、补贴、ROI 和竞对压力，不因竞对补贴更深直接建议跟随。
- [x] A-3+X、B-4+X、C-4+X 按同一口径比较券档结构、预算、结算、增量 GMV、ROI、风险和适用条件。
- [x] 波次预算、专项子预算、风险准备金、团队确认、审批立项和保密治理状态完整展示，且不触发真实配置。
- [x] 右侧章节锁定、排队、生成、完成和等待输入状态与中栏对话同步，最终产物为脱敏 Mock HTML 报告和待审批策略包。

## 最新 P1 数据完整性与来源语义

- [x] 切换任一非默认活动后，`profile.expansionRate` 与 `after` 均来自当前活动、年份和波次 profile，不继承默认活动或硬编码兜底。
- [x] 当前波次、下一波次、差异矩阵、批次详情和会话上下文中的批次字段不显示、拼接或序列化 `undefined`。
- [x] 批次字段缺值统一显示“暂无数据”或“不适用”，且不伪造批次值或精确数据。
- [x] 促前来源仅展示不可反推的脱敏来源名称，不包含链接样式、链接角色、外链图标或跳转交互。
- [x] 促前来源标签不可点击且不可作为链接聚焦，键盘操作不会触发外部导航。
- [x] 页面、数据模型、报告、附件和会话上下文均不包含真实 URL。
- [x] 非默认活动、批次缺数和来源展示场景完成浏览器回归，页面与会话语义一致。

## 全量回归与 GitHub 发布

- [x] JavaScript 语法、静态重复 ID、敏感信息与真实 URL 扫描、目标文案和 `git diff --check` 全部通过。
- [x] 浏览器全量验证七步逐章主链、三栏布局、下钻追问、3+X/4+X、预算治理、报告产物和脱敏 Mock 边界。
- [x] 浏览器验证非默认活动 profile、`expansionRate`/`after`、批次缺值、来源不可点击和真实 URL 清除。
- [x] 桌面宽屏及 1440px、1280px、1024px 下无横向溢出、文字截断、按钮遮挡或三栏错位。
- [x] 促前默认工作台、促中纵向双模块、证据入口、历史会话、键盘/ARIA 和控制台均无新增回归。
- [x] 发布前扫描确认不包含访问凭证、Token、真实 URL、真实批次 ID、临时文件或可反推业务明细。
- [ ] 验收通过后使用非强制推送发布到 GitHub `main`，远端 commit 与本地 HEAD 一致。

## 促中决策链路与多方案首屏（最新增量）

> 本分组为最新增量验收口径；与旧条目冲突时，以本分组为准。以下条目均须重新验收，不沿用历史勾选结果。

- [x] 01 当前波次与 02 下一波次均为独立全宽模块，01 固定排列在 02 之前，不使用左右并列卡片。
- [x] 01 当前波次严格遵循最新规格定义的固定决策顺序，展开详情、切换筛选或改变视口均不改变节点顺序。
- [x] 01 当前波次展示完整七行六列差异矩阵，不减少、合并或隐藏任一决策维度或字段。
- [x] 差异矩阵六列统一为调整对象、调整前、调整后、决策依据、预估影响、风险/止损，七行内容均使用同一口径。
- [x] 差异矩阵内容与当前活动、年份、波次和筛选上下文一致，切换上下文后不残留上一场景数据。
- [x] “调整点 1 · 投放人群”作为独立决策节点展开，完整展示与差异矩阵同口径的六字段。
- [x] “调整点 2 · 灵活渠道”作为独立决策节点展开，完整展示与差异矩阵同口径的六字段。
- [x] 两个调整点可分别展开和收起，状态、内容及方案上下文互不覆盖，且不会改变 01 的固定决策顺序。
- [x] 投放人群与灵活渠道的调整动作均细化到适用对象、券角色/档位、渠道/场域、端口和时间窗口。
- [x] 02 下一波次首屏同屏展示至少两套完整方案，不以仅含标题或摘要的候选卡代替完整方案。
- [x] 均衡补量、钱效优先和增长优先三套候选全部保留，均可查看、切换、追问和继续调整。
- [x] 每套完整方案均展示预算、券档、批次、发放/使用窗口、限领频控、端口、人群、场域、实验、核心指标、风险和止损线。
- [x] 同屏方案使用统一口径展示差异，推荐候选有明确标记，非推荐候选不被折叠为不可比较的摘要。
- [x] 方案首屏只保留决策所需差异、关键配置、核心指标、风险和止损线，证据表与原始数据下沉到方案之后。
- [x] 批次明细、完整指标口径、来源说明及实现性技术详情下沉，不抢占 01/02 决策首屏。
- [x] 下沉后的趋势、竞对、实验、领券核销、投放矩阵和其他证据入口保持原有筛选、下钻及数据追问交互。
- [x] 每个非默认活动均提供均衡补量、钱效优先和增长优先三套完整方案，不缺候选、不借用默认活动方案。
- [x] 非默认活动的 `profile.expansionRate`、`after` 及方案字段均来自当前活动、年份和波次 `profile`，不存在固定下标或硬编码兜底。
- [x] 切换活动、年份、波次或候选后，矩阵、两类调整点、三套方案、详情和会话上下文同步刷新且口径一致。
- [x] 当前波次、下一波次、差异矩阵、方案详情、批次详情和会话上下文均不显示、拼接或序列化 `undefined`。
- [x] 批次缺值统一显示“暂无数据”或“不适用”，不伪造批次别名、窗口、预算或其他精确值。
- [x] 来源统一展示为不可反推、不可点击且不可按链接聚焦的脱敏标签，不包含真实 URL、外链图标或跳转交互。
- [x] 差异矩阵、候选切换和两个调整点的展开控件具有可读名称，选中/展开状态与 `aria-selected`、`aria-expanded` 同步。
- [x] 候选切换、调整点展开/收起、追问和继续调整支持 Enter / Space 键盘操作，焦点顺序与视觉决策顺序一致。
- [x] 当前波次与三套下一波次入口各自携带对应活动、年份、波次、候选、完整配置、指标、风险和来源摘要，内容彼此隔离。
- [x] Task 34 对抗场景中，用户前序对人群、渠道、券档或预算的调整会持久化，并真实影响后续多轮回答。
- [x] Task 34 的典型三维交叉下钻返回与当前方案一致的数值、口径和证据，不退化为无数据支撑的固定模板。
- [x] Task 34 的券角色、行业/价格带和端口等细分维度下钻均保持当前上下文，不丢失维度或串入其他候选。
- [x] Task 34 的预算、库存、频控、互斥或护栏冲突可被识别并解释，回答给出可执行的取舍或回滚边界。
- [x] Task 34 的未建模或缺数场景明确说明“暂无数据”或“不适用”，不编造结论、批次值或精确指标。
- [x] Task 34 覆盖 618、双11等非默认活动，回答不命中固定“300-40 / 高价值 X 券”等与当前 `profile` 无关的话术。
- [x] Task 34 连续至少三轮调整、下钻和约束追问后仍停留在当前独立会话，且可恢复原方案。
- [x] JavaScript 语法、静态重复 ID、敏感信息与真实 URL、目标文案、批次 `undefined` 和 `git diff --check` 检查全部通过。
- [x] 浏览器覆盖默认及非默认活动、不同年份/波次、三候选、七行六列矩阵、两个独立展开点和至少两套完整方案同屏。
- [x] 浏览器覆盖桌面宽屏及 1440px、1307px、1280px、1024px，确认无横向溢出、文字截断、按钮遮挡或决策顺序错位。
- [x] 浏览器验证键盘/ARIA、来源标签不可点击、批次缺值、证据与技术详情下沉、控制台和网络请求均无新增回归。
- [x] 浏览器回归 KPI、趋势、归因、竞对等旧证据入口的数据追问语义，以及促前默认工作台和既有历史会话门禁。

## Round7 验收回归

- [x] 人群 / 渠道自然语言 patch 可识别并累计，且影响后续下钻。
- [x] 促前人群 / 渠道 Tab 可切换，渠道追问返回渠道数据。
- [x] 动态 Tab 运行时 ID 全局唯一并保持 ARIA 引用。
- [x] 来源 / 报告恢复明确“脱敏 Mock，仅原型演示”边界。
- [x] 静态与浏览器复验通过。

## 方案会话最后三个阻断

- [x] “目标”“重点”“核心人群”等泛称映射到当前 `profile` 的主投人群，并纳入累计 patch，持续影响后续回答与证据。
- [x] 冲突轮仅回滚配置 patch，用户已声明的全部硬约束保留并继续作用于后续轮次。
- [x] 三维证据包含当前上下文的曝光、领券、核销、增量 GMV 和 ROI，结果受累计 patch 影响；缺数时明确展示“暂无数据”或缺失字段，不编造精确值。
