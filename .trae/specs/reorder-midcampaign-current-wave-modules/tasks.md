# Tasks

- [x] Task 1: 建立当前波次四模块视图契约。
  - [x] SubTask 1.1: 定义“分日节奏、策略建议、批次配置、表现监控”的固定顺序与职责边界。
  - [x] SubTask 1.2: 为 AI 论据、批次详情、监控详情和维度页签补充独立视图状态及上下文切换重置规则。
  - [x] SubTask 1.3: 复用现有活动、年份、波次、端口、周期、采纳预览和数据源，不新增虚构业务字段。

- [x] Task 2: 重排当前波次四模块骨架与分日节奏。
  - [x] SubTask 2.1: 将分日节奏固定为 `01 当前波次调优` 的第一模块。
  - [x] SubTask 2.2: 将现有内容重组为建议、配置、表现三个后续全宽模块，移除独立人群和渠道一级区块。
  - [x] SubTask 2.3: 优化分日节奏表的高对比层级、关键日期状态和局部横向滚动。

- [x] Task 3: 重构 AI 策略调优建议与数据论据。
  - [x] SubTask 3.1: 将主视图收敛为人群、渠道、券档三个“当前策略 → 建议微调”项，并保留影响、风险和止损信息。
  - [x] SubTask 3.2: 集中展示 ROI、增量 GMV、无效成本变化及其推演/缺数状态。
  - [x] SubTask 3.3: 实现默认收起的数据论据区，以及人群、渠道、券档三个互斥页签。
  - [x] SubTask 3.4: 将现有人群表、人群与券档交叉表、渠道表和券档表迁入对应论据页签，并生成与当前维度一致的简短 AI 洞察。
  - [x] SubTask 3.5: 保持追问、采纳为灰度候选、取消预览和策略详情上下文一致，采纳不修改事实数据。

- [x] Task 4: 重构当前券批次详细配置。
  - [x] SubTask 4.1: 将批次模块改为摘要常显、详细配置默认收起。
  - [x] SubTask 4.2: 摘要仅展示配置总数、类型构成、当前端口和当前周期。
  - [x] SubTask 4.3: 展开后保留正式、储备、实验与对照配置及现有明细字段、缺数规则和采纳预览标记。
  - [x] SubTask 4.4: 从配置摘要中移除领券、核销、预算和 GMV 等重复表现指标。

- [x] Task 5: 重构当前券批次表现与实时监控。
  - [x] SubTask 5.1: 在模块首屏集中展示领券 UV、核销 UV、用户核销率、核销预算、支付 GMV 及可用的兑换比/ROI。
  - [x] SubTask 5.2: 将实时监控改为默认收起，并保留九个现有分维度下钻入口。
  - [x] SubTask 5.3: 让当前维度事实表与对应 AI 数据洞察同步切换，缺数时展示明确空态且不跨上下文补值。
  - [x] SubTask 5.4: 保持采纳预览后的 ROI、核销率、预算流速和重复触达护栏提示。

- [x] Task 6: 统一视觉层级、响应式与无障碍交互。
  - [x] SubTask 6.1: 统一四模块的标题、边框、间距、状态、展开按钮和操作区，沿用当前章节主题色。
  - [x] SubTask 6.2: 减少多层卡片嵌套，在桌面使用紧凑双栏，在 1024px 及以下保持同序纵向布局。
  - [x] SubTask 6.3: 为论据、批次、监控展开和两组页签补齐键盘操作、焦点恢复及 ARIA 状态同步。
  - [x] SubTask 6.4: 保证节奏表、页签和宽表只在自身容器内滚动，页面无整体横向溢出。

- [x] Task 7: 执行静态、浏览器和回归验证。
  - [x] SubTask 7.1: 运行 JavaScript 语法、静态与运行时重复 ID、`undefined`、敏感信息和 `git diff --check` 检查。
  - [x] SubTask 7.2: 验证 38节、618、双11及至少两个不同波次、主端/独立端、全周期/具体日期的数据隔离和空态。
  - [x] SubTask 7.3: 验证四模块顺序、三个展开区、两组页签、采纳/取消预览、追问和焦点行为。
  - [x] SubTask 7.4: 验证桌面宽屏、1440px、1280px、1024px、860px、760px 下的布局、局部滚动与 Axe。
  - [x] SubTask 7.5: 回归 `02 下一波次方案明细与调优`、外层 `03`、促前工作台和既有历史会话。

- [x] Task 8: 修复页面 DOM 可序列化内部 URL 的敏感信息泄露。
  - 失败证据：静态扫描命中 `index.html:20792` 的飞书内部表格 URL，以及 `index.html:31298-31300` 的 3 个 `ecop.bytedance.net` URL；在 `http://127.0.0.1:18765/index.html?mode=mid&validation=task7-static` 的运行时 `document.documentElement.outerHTML` 中可复现相同 4 个内部 URL，其中 3 个包含真实 `cjSiteCode=St12403140000002`。
  - 复验要求：静态 URL 扫描与运行时 DOM 序列化均不得出现内部域名、真实站点标识或执行 ID；继续覆盖页面、弹层、会话和审计快照；修复后从 Task 7 静态检查起重新执行完整 checklist，且不得以隐藏元素或未打开入口作为通过依据。

- [x] Task 9: 修复当前波次“展开数据论据”按钮的严重级文字对比度违规。
  - 失败证据：在 `http://127.0.0.1:18765/index.html?mode=mid&validation=task8-second-independent&fresh=1` 默认促中页面临时注入并运行 `axe-core 4.10.3`，`#midCurrentEvidenceToggle` 命中 `color-contrast`（impact=`serious`）；前景色 `#72798b`、背景色 `#f8f9fc`、12px 粗体的对比度为 `4.13:1`，低于要求的 `4.5:1`。
  - 复验要求：修复后从 Task 7 静态检查起重新执行全部 74 个 checklist；在默认、展开、收起、可用与缺数状态及宽屏、1440、1280、1024、860、760 视口重新运行可用 axe-core，证明无 critical/serious 违规，并复核按钮的 hover、focus-visible、disabled 和 aria-expanded 状态不会降低对比度。


- [x] Task 10: 修复 Axe 4.10.3 在响应式与缺数态的 serious 文字对比度违规。
  - 失败证据：在 `http://127.0.0.1:18765/index.html?mode=mid&validation=task7-axe-*` 注入并运行 `axe-core 4.10.3`；默认 1024px 与 860px 均命中 2 个 `color-contrast` serious 节点：当前波次 AI 建议“独立追问”按钮为 `#72798b` / `#f8f9fc`、`4.13:1`，策略详情按钮为 `#72798b` / `#ffffff`、`4.35:1`，均低于 `4.5:1`。
  - 失败证据：默认 760px 命中 38 个 `color-contrast` serious 节点，包括 12 个三条 AI 微调的字段标签（`3.62:1—3.93:1`）、3 个预估状态（`4.43:1`）、5 个当前波次操作按钮（`4.13:1—4.35:1`）、8 个批次摘要标签/说明（`3.59:1—4.24:1`）、2 个监控年份按钮（`3.74:1—4.15:1`）、6 个效率洞察操作按钮（`3.74:1—4.13:1`）及 2 个竞对操作按钮（`4.13:1—4.35:1`）。
  - 失败证据：切到第二波缺数态后，当前波次与下一波次两个空态均为 `#737d8f` / `#fafbfc`、`4.00:1`，产生 2 个 serious 节点；同轮默认 1728/1440/1280、AI 论据/批次详情/实时监控分别展开、三者同时展开及独立端缺数态均为 critical=`0`、serious=`0`。
  - 复验要求：不得通过缩小 Axe 审计范围、忽略节点或只验证桌面默认态规避；修复后用 `axe-core 4.10.3` 重跑默认六档视口、三个单独展开、三者同时展开、独立端与无可用波次缺数态，所有场景 critical/serious 必须为 `0`，并继续完成当前未勾的促前默认工作台及促前/促中历史会话回归；仅全部通过后方可勾 Task 7 与 7.1—7.5。


- [x] Task 11: 恢复当前波次 AI 论据、批次详情与实时监控模块的运行时渲染，并重新完成 Task 10/Task 7 收口验证。
  - 失败证据：在 `http://127.0.0.1:18910/index.html?mode=mid&validation=task10-axe-*` 使用 `axe-core 4.10.3` 对全页 `document` 运行 12 个场景且未配置忽略或排除节点；默认六档、独立端和第二波缺数态共 8 个场景通过，但 AI 论据展开、批次详情展开、实时监控展开、三者同时展开共 4 个场景设置失败，因此 Axe checklist 项不能勾选。
  - 失败证据：运行时 `[data-mid-current-evidence-toggle]`、`[data-mid38-batch-toggle]`、`[data-mid38-monitor-toggle]` 数量均为 `0`，当前章节 `[data-mid38-block]` 仅剩 `rhythm`；四个失败场景虽在实际渲染的默认 DOM 上均得到 critical=`0`、serious=`0`，但没有审计到 Task 10 要求的展开内容。
  - 根因证据：`renderMidCurrentAiRecommendation`、`renderMid38BatchBlock`、`renderMid38MonitorBlock` 仍有定义但无调用点；当前 `renderMidRecommendationHub` 只渲染波次摘要、差异调优表和完整只读方案，覆盖了原四模块运行时输出。
  - 复验要求：不得以“控件不存在”或只审计默认 DOM 作为通过；恢复后先证明三个展开控件各恰有一个、四模块顺序完整，再用 `axe-core 4.10.3` 无排除地重跑全部 12 场景，并继续完成促前默认工作台及促前/促中历史会话回归；仅全部通过后方可勾 Axe、最后回归项及 Task 7/7.1—7.5。

# Task Dependencies

- Task 1 无前置依赖。
- Task 2 依赖 Task 1。
- Task 3、Task 4、Task 5 依赖 Task 1 和 Task 2，三者可并行实施。
- Task 6 依赖 Task 3、Task 4、Task 5。
- Task 7 依赖 Task 2 至 Task 6。
