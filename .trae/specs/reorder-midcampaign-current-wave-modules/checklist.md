# 验收清单

## 整体信息架构

- [x] `01 当前波次调优` 固定按“分日节奏 → 策略建议 → 批次配置 → 表现监控”排列。
- [x] 四个模块均为独立全宽区块，不使用左右并列的一级模块。
- [x] 四个模块不新增与外层章节冲突的 `01 / 02 / 03 / 04` 视觉编号。
- [x] 页面不存在独立的“调人群”或“调渠道”一级区块。
- [x] `02 下一波次方案明细与调优` 和外层 `03` 的顺序、编号及内容未被修改。

## 当前券波次分日节奏

- [x] 分日节奏是 `01` 的第一模块且默认可见。
- [x] 日期、节奏、发券形式、固定渠道/全人群和灵活渠道完整保留。
- [x] 关键日期使用当前章节主题色的高对比强调。
- [x] 日期选择与“查看全周期”继续更新当前周期上下文。
- [x] 窄屏仅表格容器横向滚动，页面无整体横向溢出。

## AI 策略调优建议

- [x] 建议主视图只包含人群、渠道、券档三个微调方向。
- [x] 每个微调项展示调整对象、“当前策略 → 建议微调”、预期影响和风险/止损。
- [x] 券档建议保持限制泛发、定向验证、稳量或角色调整等微调语义。
- [x] 不展示无来源的大幅预算变更或精确切量比例。
- [x] ROI、增量 GMV、无效成本变化集中展示并标明预估状态。
- [x] 缺失预估显示“待灰度验证”或“暂无数据”，不跨上下文补值。
- [x] 追问、采纳、取消预览和策略详情继续可用。
- [x] 采纳只生成可撤销预览，不修改事实数据或触发真实配置。

## AI 数据论据下钻

- [x] 数据论据默认收起，并只有一个主展开入口。
- [x] 展开后提供人群、渠道、券档三个互斥页签，默认打开人群。
- [x] 人群页签展示领券、核销、预算、GMV 占比、核销率及人群与券档交叉事实。
- [x] 渠道页签展示领券占比、核销率、预算占比、GMV 占比和领券成本。
- [x] 券档页签展示领券、核销、核销率、预算占比、GMV 占比和客单。
- [x] 每个页签只展示与当前维度直接相关的简短 AI 洞察。
- [x] 证据不足时显示明确空态，不沿用其他端口、周期、波次或维度结论。
- [x] 收起或切换上下文后，下次展开恢复默认人群页签。

## 当前券批次详细配置

- [x] 配置模块位于 AI 建议之后、表现监控之前。
- [x] 详细配置默认收起。
- [x] 摘要只展示配置总数、类型构成、当前端口和当前周期。
- [x] 摘要不重复展示领券、核销、预算和 GMV 指标。
- [x] 展开后保留正式主批次、储备与领前增发、实验与对照三类配置。
- [x] 明细保留批次别名、业务角色、端口、券类型、门槛-面额、状态及现有可用表现字段。
- [x] 缺失字段显示“暂无数据”或“不适用”，不出现 `undefined`。
- [x] 采纳预览标记不覆盖原始配置和值。

## 当前券批次表现与实时监控

- [x] 核心指标在第四模块首屏集中展示。
- [x] 核心指标包含领券 UV、核销 UV、用户核销率、核销预算和支付 GMV。
- [x] 兑换比或 ROI 只在存在同口径数据时展示。
- [x] 指标缺失显示“暂无数据”，不补零、不跨上下文回填。
- [x] 实时监控与分维度下钻默认收起。
- [x] 展开后保留分天、分天分小时、分档位、分渠道、分行业、分体裁、分消费力等级、分等级增发比例和分端口。
- [x] 同一时刻只展示一个监控维度，默认打开分天。
- [x] 维度切换后事实表和 AI 数据洞察同步更新。
- [x] AI 洞察不把相关性表述为已验证因果。
- [x] 缺少当前维度证据时不展示其他维度或其他上下文洞察。
- [x] 采纳预览后的 ROI、核销率、预算流速和重复触达护栏仍可见。

## 状态与数据边界

- [x] 四个模块只读取当前活动、年份、波次、端口和周期数据。
- [x] 切换任一上下文后四个模块原子刷新，不残留旧数据。
- [x] 上下文切换后 AI 论据、批次详情和监控详情恢复默认收起。
- [x] 上下文切换后采纳预览自动清除。
- [x] 缺数波次不展示其他波次的建议、配置、指标或洞察。
- [x] 页面、弹层、会话和审计快照不包含内部 URL、真实执行 ID 或可反推来源。

## 视觉与响应式

- [x] 四模块统一使用当前 `01` 章节主题色，不分别引入冲突色彩。
- [x] 区块标题、边框、圆角、间距、状态和操作区视觉一致。
- [x] 页面没有不必要的多层卡片嵌套和重复说明文字。
- [x] 桌面宽屏、1440px、1280px 下信息密度平衡，无大面积空白或文本堆砌。
- [x] 1024px、860px、760px 下四模块顺序不变，AI 建议和预估效果转为纵向排列。
- [x] 所有宽表与页签只在自身容器内滚动。
- [x] 各视口无文字截断、按钮重叠、标题错位或页面横向溢出。

## 键盘与无障碍

- [x] AI 论据、批次详情和实时监控支持 Enter/Space 展开与收起。
- [x] 人群/渠道/券档及监控维度页签支持方向键、Home/End、Enter/Space。
- [x] `aria-expanded`、`aria-selected`、`aria-controls`、`tabindex` 与视觉状态同步。
- [x] 重渲染、收起和切换上下文后焦点不会落回 `body` 或跳到其他章节。
- [x] 采纳状态和缺数状态具有可读文本或 `aria-live` 反馈。
- [x] Axe 无新增 critical 或 serious 违规。

## 静态与回归

- [x] JavaScript 语法检查通过。
- [x] 静态与运行时重复 ID 检查通过。
- [x] `git diff --check` 通过。
- [x] 浏览器控制台无新增错误。
- [x] DOM、会话和审计快照不显示或序列化 `undefined`。
- [x] 38节、618、双11及至少两个不同波次验证通过。
- [x] 主端/独立端、全周期/具体日期的数据和空态验证通过。
- [x] `02 下一波次方案明细与调优` 无回归。
- [x] 外层 `03` KPI、趋势、实验、竞对和归因交互无回归。
- [x] 促前默认工作台和既有促前/促中历史会话无回归。
