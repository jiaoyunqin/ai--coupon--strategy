# 验收清单

## 范围与边界

- [x] 本次只增强 `03 促前情报与机会研判` 的现有卡片，不新增主章节或独立大模块。
- [x] `01 / 02 / 03 / 04` 主章节编号和顺序不变。
- [x] `04 历史实验下钻与 AI 洞察` 的 DOM、内容、状态、筛选、展开、下钻和追问逻辑未修改。
- [x] 不新增真实数据接入、外部链接、第三方依赖、字体、图标库或图片。
- [x] 新增展示全部复用当前活动、参考促、候选、洞察和竞对归档数据。

## AI 收敛结论

- [x] 顶部摘要依次展示“水位判断 / 当前方案影响 / 待验证风险”。
- [x] 摘要不再出现“下一步生成 3+X / 4+X 候选”等过时文案。
- [x] 当前方案影响明确显示当前选中的方案一、方案二或方案三。
- [x] 切换候选后当前方案影响同步刷新。
- [x] 水位判断继续来自当前活动和参考促重算后的洞察。
- [x] 待验证风险明确表达数据边界，不伪造实时货盘、库存或精确结论。
- [x] 无可用证据时展示空态，不引用其他活动数据。
- [x] 顶部追问入口继续可用，追问摘要与当前判断一致。

## 自然水位卡

- [x] 自然水位卡展示 GMV、DAC、客单三个紧凑同比摘要。
- [x] GMV / DAC / 客单 Tab 保留并具有清晰选中态。
- [x] D-20 至 D-1 趋势图保留。
- [x] 点击指标后图表、摘要、解释和 ARIA 状态同步。
- [x] 点击任一趋势点后当前数据点、变化判断和方案影响同步。
- [x] 趋势点支持 Enter / Space，指标 Tab 支持方向键和 Home / End。
- [x] 不新增无来源异常点、实时数据或精确指标。
- [x] 追问入口继续携带当前指标、当前点和水位判断。

## 机会与效率卡

- [x] 卡片提供 `类目机会 / 券档漏斗 / 行业价格带` 三个内部 Tab。
- [x] 默认视图仍为类目机会。
- [x] 类目机会保留排行、GMV 指数、DAC/客单和焦点动作。
- [x] 券档漏斗展示券档、发放、领取、核销、领取率、领后核销和 ROI。
- [x] 券档漏斗使用双层视觉编码区分领取率与领后核销。
- [x] 点击券档后展示对应诊断和建议动作。
- [x] 行业价格带视图提供行业/价格带二级切换。
- [x] 行业视图展示历史客单、适配券档和领后核销。
- [x] 价格带视图展示领取率、领后核销和历史表现。
- [x] 券档、行业和价格带数据均来自当前 `currentInsights`。
- [x] 机会卡不复制 `04` 的证据、来源和适用边界全文。
- [x] 三个视图和二级切换支持鼠标与键盘操作。
- [x] 当前视图、焦点条目、追问上下文和推荐问题保持一致。
- [x] 缺少券档或下钻数据时展示明确空态。

## 竞对卡

- [x] 竞对卡提供 `动态播报 / 横向对比` 两个视图。
- [x] 默认视图为动态播报。
- [x] 动态播报保留年份、影响等级、变化前后、动作、反馈和关注能力。
- [x] 横向对比同屏展示竞对 A/B/C。
- [x] 横向对比统一展示券档、折扣、会员/张数、频次和变化方向。
- [x] 横向对比包含当前年份的压力判断。
- [x] 年份切换后当前竞对视图保持不变，内容同步刷新。
- [x] 竞对内容不出现真实平台名称或可反推数据。
- [x] 竞对追问上下文与当前年份和当前视图一致。
- [x] 竞对双视图支持键盘操作并同步 ARIA 状态。

## 视觉与响应式

- [x] `03` 内新增装饰色全部使用琥珀色章节主题变量。
- [x] 成功、警告、危险和禁用状态继续使用语义状态色。
- [x] AI 摘要、自然水位、机会效率和竞对卡具有一致的边框、圆角、阴影和间距。
- [x] 核心结论和指标视觉权重高于 Tab、追问和辅助说明。
- [x] 页面不出现长段说明文字堆叠。
- [x] 桌面宽屏和 1440px 下双列分析卡对齐。
- [x] 1280px 下无文字裁切、按钮遮挡或整页横向溢出。
- [x] 1024px、860px、760px 下分析卡按现有规则降为单列。
- [x] 紧凑矩阵或 Tab 仅在自身容器内滚动。
- [x] `prefers-reduced-motion` 下无非必要位移动画。

## 可访问性与语义

- [x] `03` 主标题继续使用 H2，卡片标题继续使用 H3。
- [x] 新增 Tab、二级切换和视图容器具有稳定唯一 ID。
- [x] `aria-selected`、`aria-pressed`、`aria-controls`、`tabindex` 与视觉状态同步。
- [x] 所有追问、切换和焦点条目具有可读名称。
- [x] 方向键、Home、End、Enter 和 Space 不造成二次激活。
- [x] 焦点在动态重渲染后恢复到对应控件。
- [x] 页面不存在按钮嵌套或不可聚焦的伪按钮。
- [x] Axe critical 和 serious 均为 0。

## 上下文、空态与回归

- [x] 切换活动后 `03` 的水位、类目、券档、行业/价格带和竞对同步刷新。
- [x] 改变参考促集合后 `03` 的指标和小结同步重算。
- [x] 切换候选只更新方案影响，不改变历史水位和证据上下文。
- [x] 无参考促时 AI 摘要、趋势、机会和竞对均显示明确空态或禁用状态。
- [x] 缺数状态不回退或拼接默认活动旧数据。
- [x] `02` 候选方案、追问、建议采纳和营销配置入口无回归。
- [x] `04` 分类筛选、洞察展开、行业/价格带下钻和追问无回归。
- [x] “促前券档位设计”历史对话七步门禁无回归。
- [x] 促中工作台和“促中预算紧张”历史对话无回归。

## 静态与浏览器验证

- [x] 内联 JavaScript 语法检查通过。
- [x] 静态和运行时重复 ID 检查通过。
- [x] `git diff --check` 通过。
- [x] 浏览器控制台无新增页面错误。
- [x] 网络请求无新增异常。
- [x] 默认活动和至少一个非默认活动完成浏览器验证。
- [x] 候选切换、指标切换、趋势点、机会三视图、行业/价格带、竞对双视图和年份筛选全部完成浏览器验证。
