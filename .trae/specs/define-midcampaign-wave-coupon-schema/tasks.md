# Tasks

- [x] Task 1: 建立统一的波次券方案 Mock 数据契约。
  - [x] SubTask 1.1: 定义波次总览、券方案单元、渠道规则、实验、护栏和调整项结构。
  - [x] SubTask 1.2: 为工作簿事实、Mock 推演和用户调整结果增加明确来源类型。
  - [x] SubTask 1.3: 使用业务别名关联对象，排除配置 ID、活动 ID、投放计划 ID、点位数字 ID、BSK 和内部链接。

- [x] Task 2: 按统一契约生成当前波次 Mock。
  - [x] SubTask 2.1: Mock 波次总览及通发、增发、互动/膨胀等券方案单元。
  - [x] SubTask 2.2: Mock 券方案单元对应的渠道动作、实验关系和能力限制。
  - [x] SubTask 2.3: Mock 当前事实、建议调整、影响范围、预估效果、联合护栏和回滚条件。
  - [x] SubTask 2.4: 保留至少一个缺数或待灰度验证场景，验证数据降级。

- [x] Task 3: 按统一契约生成下一波次候选 Mock。
  - [x] SubTask 3.1: 为现有候选方案生成完整波次总览和券方案单元。
  - [x] SubTask 3.2: 为每个候选生成渠道动作矩阵、实验与护栏。
  - [x] SubTask 3.3: 为可调项生成当前值、推荐值、合法范围、联动影响和风险。
  - [x] SubTask 3.4: 确保候选切换不会复用其他候选的券单元、渠道规则或实验数据。

- [x] Task 4: 将 Mock 接入 `01 当前波次调优` 和 `02 下一波次方案明细与调优`。
  - [x] SubTask 4.1: `01` 首屏展示波次摘要和差异调优表，完整方案按需下钻。
  - [x] SubTask 4.2: `02` 按“波次总览 → 券方案单元 → 渠道矩阵 → 实验护栏 → 可调项”展示选中候选。
  - [x] SubTask 4.3: 渠道矩阵单元格仅使用发券、催用、引流、仅入口和不启用五类动作。
  - [x] SubTask 4.4: 保持现有候选选择、追问、继续调整、上下文隔离和响应式交互。

- [x] Task 5: 验证数据完整性、结构一致性和界面表现。
  - [x] SubTask 5.1: 校验每个波次均包含 P0 必填对象和字段。
  - [x] SubTask 5.2: 校验工作簿事实、Mock 推演和用户调整结果没有混用。
  - [x] SubTask 5.3: 校验页面、DOM、会话和审计快照不包含技术 ID、内部链接或 `undefined`。
  - [x] SubTask 5.4: 验证当前/下一波次、候选、活动、年份和缺数上下文切换。
  - [x] SubTask 5.5: 验证桌面宽屏、1440px、1280px、1024px、860px 和 760px 下的局部滚动、键盘与 ARIA。


- [x] Task 6: 修复 Task 5 独立验收阻塞项。
  - [x] SubTask 6.1: 补齐当前波次 P1 决策目标的优先级和适用条件。失败证据：`waveOverview.objective.value` 当前为单一目标字符串，未包含独立的 priority/condition；复验要求：当前波次与三候选均以独立字段提供主目标、优先级、适用条件，并通过契约校验与页面核对。
  - [x] SubTask 6.2: 恢复 `01 当前波次调优` 的决策首屏。失败证据：最终真实浏览器首屏 `waveSummary=false`、`difference=false`，实际同时展示 AI 策略建议、19 条批次配置摘要和实时监控；复验要求：首屏仅保留波次摘要与差异调优表，未变化配置、监控和完整方案移入按需下钻，并逐列核对差异表。
  - [x] SubTask 6.3: 清理页面、完整 DOM 序列化和候选会话中的技术/敏感提示。失败证据：`document.documentElement.outerHTML` 命中 `undefined` 19 次、`Mock` 16 次、`Revision` 1 次、`BSK` 7 次、活动 ID 字段 29 次、投放计划 ID 字段 1 次、点位 ID 字段 1 次、URL 7 次；候选会话可见 `mock:38节|2026|full:next:balanced`。复验要求：页面、弹层、完整 DOM、会话与审计快照对上述词项及技术 ID/内部 URL 全部为 0，同时保留业务化的“事实/预估/已调整”来源区分。
  - [x] SubTask 6.4: 完成修复后重新执行 Task 5 全量验收。复验要求：重新运行 JavaScript 语法、重复 ID、DOM/会话/审计敏感信息、`undefined`、控制台、网络、`git diff --check`、01/02 鼠标与键盘、三候选、五 Tab、渠道矩阵以及 1600/1440/1280/1024/860/760 真实 Chrome 视口检查；全部通过后再勾选失败清单项、SubTask 5.3 与 Task 5。

# Task Dependencies

- Task 1 无前置依赖。
- Task 2 和 Task 3 依赖 Task 1，完成统一契约后可并行。
- Task 4 依赖 Task 2 和 Task 3。
- Task 5 依赖 Task 1 至 Task 4。
