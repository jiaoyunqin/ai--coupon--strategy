# 未执行状态

- [x] 首次进入“促前券档位设计”时，右侧生成队列显示完整七步。
- [x] 0 个完成章节时显示“尚未生成报告”空态。
- [x] 0 个完成章节时不显示报告封面、目标约束正文、来源卡片或锁定章节。
- [x] 排队、分析和写入状态只显示在生成队列和紧凑执行提示中。
- [x] 生成失败或取消后不保留半成品章节。

# 渐进式报告

- [x] 用户确认目标与计划后，第 1 章首次写入正式报告，进度为 1/7。
- [x] P0 完成后第 2 章写入，进度为 2/7。
- [x] 后续章节按既有七步顺序逐章写入。
- [x] 正式报告只包含状态为 `complete` 且未隐藏的章节。
- [x] `locked`、`queued`、`generating`、写入中和 `needs-input` 章节不进入正式报告。
- [x] 新章节以一次性提交方式写入，不展示逐字段跳动或伪造正文。
- [x] 新章节写入后不强制切换报告模式或抢夺用户焦点。
- [x] 章节写入动效克制，并遵循减少动态效果偏好。

# 工具栏门禁

- [x] 0 个完成章节时，“查看报告”“自定义”“全屏”、HTML 预览和下载不可用。
- [x] 禁用控件具有 `aria-disabled` 和可读原因。
- [x] 1-6 个完成章节时，可以查看和全屏当前草稿。
- [x] 1-6 个完成章节时，自定义面板只列出已完成章节。
- [x] 1-6 个完成章节时，最终 HTML 下载不可用。
- [x] 7 个章节全部完成且无待更新章节时，HTML 预览和下载可用。
- [x] 队列中只有已完成章节可以定位到正式报告正文。
- [x] 点击未完成章节时只反馈依赖，不切换到空报告。

# 更新与一致性

- [x] 修改目标、预算或口径后，受影响章节标记为“待更新”。
- [x] 存在待更新章节时，报告顶部显示轻量提示并禁用最终下载。
- [x] 重算完成后在原章节位置更新，不生成重复章节。
- [x] 隐藏和恢复操作只作用于已完成章节。
- [x] 自定义报告时至少保留一个已完成章节。
- [x] 报告封面进度与生成队列完成数一致。
- [x] 中栏对话状态、生成队列和正式报告内容一致。

# 响应式与可访问性

- [x] 桌面宽屏、1280px 和 1024px 下无文字遮挡或横向溢出。
- [x] 移动端 0/7 时报告抽屉只显示生成队列和空态。
- [x] 移动端部分完成时可以切换生成队列和当前报告草稿。
- [x] 键盘可操作队列、报告模式切换和可用工具。
- [x] 动态状态通过可读文本和 `aria-live` 反馈，不只依赖颜色。
- [x] 新章节写入和工具解锁不会造成非预期焦点跳转。

# 回归

- [x] 促前目标约束、P0、P1、核销、实验竞对、候选和治理七步门禁保持不变。
- [x] 已完成章节的业务内容、图表、表格、来源和追问能力保持不变。
- [x] 最终自包含 HTML 报告和待审批策略包仍可生成。
- [x] “促中预算紧张”历史会话不受影响。
- [x] 促前默认工作台 01-04 章节编号和交互不受影响。
- [x] 新建策略对话推荐卡复刻行为不受影响。
- [x] JavaScript 语法检查通过。
- [x] 静态 ID 无重复。
- [x] 浏览器控制台无新增错误。
- [x] `git diff --check` 通过。
