# Tasks

- [x] Task 1: 建立核心界面共享设计 token。
  - [x] SubTask 1.1: 在 `:root` 中补齐语义字号、间距、圆角、阴影、文本色、边框色、表面色和控件高度 token。
  - [x] SubTask 1.2: 将促前、促中、历史对话、新建对话和共享侧栏的公共组件映射到共享 token。
  - [x] SubTask 1.3: 保留章节色和业务状态色的语义边界，不改变现有状态判断。

- [x] Task 2: 提升核心文本和操作目标可读性。
  - [x] SubTask 2.1: 将核心业务正文、指标标签、时间、预算、风险和辅助说明迁移到 10 / 11 / 12px 语义层级。
  - [x] SubTask 2.2: 将 H2、H3、H4 统一为 20px、15–16px、13–14px 层级。
  - [x] SubTask 2.3: 将标准控件、选择器、Tab、行内动作和图标按钮提升到规定的最小点击尺寸。
  - [x] SubTask 2.4: 通过换行、纵向排列和容器内滚动吸收字号与控件高度变化，不截断关键业务内容。

- [x] Task 3: 修复标题层级和重复标题。
  - [x] SubTask 3.1: 为促前方案工作区补齐 H3 语义层，消除 H2→H4 跳级。
  - [x] SubTask 3.2: 收敛促中 `03.1` 的重复指标标题，并检查促前、促中其他可见标题重复。
  - [x] SubTask 3.3: 保持动态标题、章节编号、标题 ID 和 `aria-labelledby` 在重渲染后稳定。

- [x] Task 4: 统一核心图标、术语和标点。
  - [x] SubTask 4.1: 建立可复用的内联 SVG 图标模板，覆盖追问、返回、添加、关闭、全屏、展开和下载。
  - [x] SubTask 4.2: 将核心界面的 `✦ / ← / ＋ / × / ⛶ / ↓` 操作字符替换为统一图标，保持原 ID、事件和可访问名称。
  - [x] SubTask 4.3: 将侧栏和报告中的非必要英文改为中文，保留必要业务与技术缩写。
  - [x] SubTask 4.4: 统一元数据、日期、维度组合、状态变化和普通并列关系的标点规则。

- [x] Task 5: 收敛核心卡片与操作层级。
  - [x] SubTask 5.1: 将核心章节、一级内容卡和内部信息组分别映射到统一边框、圆角和阴影层级。
  - [x] SubTask 5.2: 减少卡片套卡片和重复渐变、重阴影，不改变信息顺序或数据内容。
  - [x] SubTask 5.3: 每个区域保留一个明确主操作，追问、展开和查看证据降为辅助操作样式。
  - [x] SubTask 5.4: 统一促前、促中和历史对话的状态标签、Tab、筛选器和按钮反馈。

- [x] Task 6: 完善共享侧栏和对话工作区。
  - [x] SubTask 6.1: 为 1280px 以下图标侧栏补充悬停和聚焦 tooltip。
  - [x] SubTask 6.2: 统一新建对话、促前历史对话和促中历史对话的顶栏、消息、输入区及工具按钮。
  - [x] SubTask 6.3: 统一报告生成队列和工具栏术语、图标和控件尺寸。
  - [x] SubTask 6.4: 基础能力管理页仅继承共享字体、控件和导航规则，不重排其业务布局。

- [x] Task 7: 执行静态、视觉和交互回归。
  - [x] SubTask 7.1: 检查 inline JavaScript、静态与运行时重复 ID、标题层级、ARIA 引用、敏感信息和 `git diff --check`。
  - [x] SubTask 7.2: 统计默认促前和促中可见文本字号与操作目标尺寸，确认核心业务文本不低于 10px、规定控件达到最小高度。
  - [x] SubTask 7.3: 验证桌面宽屏、1440px、1280px、1024px、860px、760px 和 200% 缩放下无整页溢出、遮挡或关键文本截断。
  - [x] SubTask 7.4: 使用 Axe 和人工键盘测试验证对比度、焦点、tooltip、Tab、弹层和图标按钮可访问性。
  - [x] SubTask 7.5: 回归促前、促中、缺数波次、三方案、历史对话、新建对话、报告和基础能力入口。

- [x] Task 8: 修复核心视觉独立验收阻断。
  - [x] SubTask 8.1: 使用本地 Axe 4.12.1 复现并修复默认促前、新建对话、促前历史和促中历史的 serious 语义、可聚焦滚动区与对比度问题。
  - [x] SubTask 8.2: 将促前 SVG 图表点的实际操作目标提升到至少 32×32px，并确保促中主操作在标准与 compact 规则下不低于 36px。
  - [x] SubTask 8.3: 修复促前候选指标“结算预算”、860px 活动名和促中下一波次策略正文的截断，允许内容换行和卡片增高。
  - [x] SubTask 8.4: 为静态与动态 `.pre-insight-toggle` 补齐 `iconSVG("expand")`，并保持展开状态旋转与语义同步。
  - [x] SubTask 8.5: 通过末尾覆盖确保运行时核心可见伪元素字号不低于 10px，且核心业务标签不使用 10px 以下字号。
  - [x] SubTask 8.6: 恢复 `infrastructureData` 中 `ds-01`、`ds-02` 的 `name` 与 `uri` 为 HEAD 内部值，中文化仅限 `renderSourceCards` 显示层映射。
  - [x] SubTask 8.7: 仅修复本轮核心可见日期空格与明确的 V1、V15 标题标点，不改 `3+X`、`4+X`、路径或正则。
  - [x] SubTask 8.8: 为 `font-size-subsection-lg`、`font-size-card-lg` 和 `space-2xl` 建立合适的 H3、H4 与核心 shell/chapter 语义使用，或删除确实冗余的 token。
  - [x] SubTask 8.9: 保持核心指定字符 `✦ / ← / ＋ / × / ⛶ / ↓` 搜索结果为 0，并在当前可见发送按钮中使用 send SVG。

- [x] Task 9: 修复最终视觉签收阻断。
  - [x] SubTask 9.1: 恢复 `infrastructureData` 中 `ds-01`、`ds-02` 的 `description` 为 HEAD 原值，并保持数据源视觉无关字段与 HEAD 一致，中文与脱敏展示仅在 `renderSourceCards` 映射。
  - [x] SubTask 9.2: 修复 860px 下促前活动年同期名称截断，仅允许年同期对比字段换行和自适应增高，并完成 860px 完整文本与整页溢出定向验证。

# Task Dependencies

- Task 1 无前置依赖。
- Task 2、Task 3 和 Task 4 依赖 Task 1；三项可并行实施。
- Task 5 依赖 Task 1 和 Task 2。
- Task 6 依赖 Task 1 和 Task 4。
- Task 7 依赖 Task 1 至 Task 6。
