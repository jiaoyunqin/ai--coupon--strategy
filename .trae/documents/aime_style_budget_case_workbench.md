# Aime 风格「促中预算紧张」工作台改造计划

## Summary

本次仅重构“促中预算紧张”案例，不改变默认入口和原促中盯盘工作台。用户仍从现有工作台进入历史对话；进入案例后，主区域切换为 Aime 风格的任务工作台：

* 左侧：沿用现有 Agent 导航和历史会话。

* 中部：完整多轮对话区，包含历史消息、AI 分析过程、推荐动作和固定输入框。

* 右侧：单份报告的渐进式生成队列与报告画布，章节由中部对话动作逐步解锁。

* 产物：分析完成后在对话中生成 HTML 文件卡；点击后在右栏预览，支持全屏、返回队列和下载自包含 HTML。

目标不是复制 Aime 的品牌样式，而是复用其信息架构和交互节奏：对话负责解释、澄清和驱动任务，报告负责承载完整结论与数据，生成状态始终可见。

## Current State Analysis

### 当前实现

项目是单文件静态页面：

* 页面、样式、数据和交互均位于 `/Users/bytedance/strategy2/index.html`。

* 默认展示原促中盯盘工作台。

* 点击左侧“促中预算紧张”后，由 `enterBudgetCase()` 隐藏原工作台并显示 `#budgetCaseView`。

* 案例数据集中在 `budgetCaseData`，状态集中在 `budgetCaseState`。

* 案例当前结构为：

  * 顶部案例标题与 6 步进度。

  * 左侧 `.case-main-stream` 承载问题卡、分析计划、数据分析卡和最终候选。

  * 右侧 310px `.case-aside` 仅承载追问记录与安全边界。

* 分析计划确认后，`runCaseAnalysisSequence()` 使用定时器自动连续生成总体、人群、规模、预算和风险模块。

* `submitCaseQuestion()` 已具备本地脚本化的四阶段反馈：

  * 理解问题

  * 定位数据

  * 完成归因

  * 输出结论

* 现有报告内容、数据口径、追问模板、预算测算、风险确认和候选审批逻辑均应保留。

### 与 Aime 参考站点的差距

已实际访问 `https://aime.bytedance.net/chat/65571788-8a88-469e-81f0-48e9d742a285`，其关键交互事实为：

* 任务列表、中部聊天、右侧产物预览三栏占满视口，各区域独立滚动。

* 中部聊天具有固定底部输入框，消息流中同时存在：

  * 用户输入

  * AI 短回答

  * 思考/修正过程

  * 操作结果

  * 链接或文件产物

* 右侧使用独立 iframe 预览生成的 HTML，预览区有顶部工具栏和关闭/展开类操作。

* 聊天区与产物区互不抢占纵向滚动，长报告不会拉长整页。

* 当前 Demo 缺少固定输入框、正式消息模型、报告章节队列、文件卡、内嵌预览、全屏和下载。

* 当前确认计划后会自动生成大部分内容，不符合已确定的“由对话动作驱动逐章生成”。

## Proposed Changes

### 1. 重构案例视图为视口内三栏工作台

**文件**

* `/Users/bytedance/strategy2/index.html`

**改动区域**

* 现有 `.case-view`、`.case-header`、`.case-layout`、`.case-main-stream`、`.case-aside` 相关 CSS。

* `#budgetCaseView` 的 HTML 结构。

* `enterBudgetCase()` 和 `exitBudgetCase()` 的视图状态处理。

**实现**

* 保留页面最左侧现有深色 Agent 导航，不重构其他页面。

* 案例模式下，移除 `.workspace` 的常规内容页留白，使案例工作台占满顶栏以下区域：

  * 高度：`calc(100vh - 68px)`。

  * 整页不产生案例内容长滚动。

* 案例主区域改为两列，连同现有导航构成视觉三栏：

  * 中部对话：`clamp(420px, 34vw, 560px)`。

  * 右侧报告：占用剩余宽度，最小 560px。

* 中栏和右栏各自使用 `min-height: 0` 与 `overflow: hidden` 建立独立滚动上下文。

* 案例标题、V1/V15 标签、累计两天状态和总进度移入中栏顶部的紧凑任务头，不再占用整页大卡。

* 顶栏保留现有品牌和返回路径；原工作台状态只做隐藏，不销毁。

**响应式**

* `> 1440px`：完整导航 + 对话 + 报告，报告区更宽。

* `1024px–1440px`：沿用现有 74px 折叠导航，对话固定约 400–440px，报告自适应。

* `< 1024px`：报告区改为覆盖式抽屉；中部对话占满主区域，通过“查看报告”按钮打开报告。

* 进入/退出案例不重置原工作台筛选、图表和关注状态。

### 2. 建立正式的多轮对话数据模型

**文件**

* `/Users/bytedance/strategy2/index.html`

**改动区域**

* `budgetCaseState`。

* 新增对话渲染和消息追加函数。

* 复用 `budgetCaseData.questions`、`budgetCaseData.answers` 与现有案例指标。

**状态结构**

在 `budgetCaseState` 中新增：

* `messages`：对话消息数组。

* `reportStep`：当前已解锁的报告章节。

* `reportSections`：各章节 `locked / queued / generating / complete / needs-input` 状态。

* `reportMode`：`queue / report / file-preview`。

* `activeReportSection`：右侧当前章节。

* `artifact`：最终 HTML 文件元数据与 Blob URL。

* `isResponding`：阻止重复提交。

消息对象使用统一结构：

* `id`

* `role`: `user / assistant / system`

* `type`: `text / plan / progress / insight / action / artifact / correction`

* `content`

* `actions`

* `timestamp`

* `contextType`

**初始会话**

进入案例后默认展示“已开始、待确认”状态：

1. 用户原始决策问题。
2. AI 对问题的重述和已知/缺失数据说明。
3. AI 分析计划卡。
4. 推荐动作：

   * 确认计划并分析总体

   * 补充分析要求

   * 修改问题

不默认展示后续数据结论。

### 3. 增加中部固定对话输入区

**文件**

* `/Users/bytedance/strategy2/index.html`

**结构**

* 中栏顶部：任务标题、状态、返回工作台。

* 中栏中部：独立滚动的消息列表。

* 中栏底部：固定 Composer。

**Composer 内容**

* 多行 `textarea`，占位文案为“继续追问，或补充预算、人群与风险要求…”。

* 发送按钮。

* 轻量工具按钮：

  * 清空当前输入

  * 模拟附件入口，仅展示 Demo 提示，不上传真实文件

* 当前阶段的推荐问题 Chips。

* Enter 发送，Shift+Enter 换行。

* 响应生成期间禁用重复发送并显示“Agent 正在分析”。

**本地脚本化回答**

* 不连接真实 AI API。

* 使用关键词与当前上下文匹配到：

  * 总体效率

  * L4/L5 与 jp/t8

  * 规模效应

  * 预算容量

  * 风险止损

  * 最终决策

* 未命中模板时返回通用回答，但必须引用当前步骤、当前报告章节与已知案例数据。

* 自由输入仍保留现有四阶段过程动画。

* 回答中的动作按钮可驱动下一报告章节，不能只显示 Toast。

### 4. 将自动分析改为对话动作驱动

**文件**

* `/Users/bytedance/strategy2/index.html`

**替换逻辑**

* 重写 `runCaseAnalysisSequence()`，取消确认后自动跑完全部章节。

* 每次对话动作只推进一个明确阶段：

| 对话动作         | 报告章节变化             | 中部反馈                 |
| ------------ | ------------------ | -------------------- |
| 确认分析计划       | “问题与计划”完成，“总体对比”生成 | 总体核心结论 + 下一步按钮       |
| 继续下钻人群       | “人群下钻”生成           | 兑换比同口径解释             |
| 拆分规模效应       | “规模归因”生成           | 6,400 万 vs 8,000 万解释 |
| 补充预算或选择参数化分析 | “预算方案”生成           | 覆盖缺口和两种候选            |
| 确认风险清单       | “风险与决策”生成          | 风险摘要与最终建议            |
| 确认灰度候选       | 全报告完成并生成文件         | 文件卡与待审批状态            |

* 每次生成均依次展示：

  * 排队

  * 生成中

  * 已完成

* 未满足前置条件的章节保持锁定，并说明依赖。

* 用户可以追问已完成章节，但追问不自动跳过后续门禁。

### 5. 新增右侧渐进式报告 Queue

**文件**

* `/Users/bytedance/strategy2/index.html`

**右栏结构**

* 顶部报告工具栏：

  * 报告标题

  * 完成章节数

  * “查看队列 / 查看报告”切换

  * 全屏

  * 关闭报告区（窄屏使用）

* 左侧章节轨道或顶部紧凑目录：

  1. 决策问题与分析计划
  2. 总体策略对比
  3. L4/L5 与 jp/t8 人群下钻
  4. 效率与规模归因
  5. 预算容量与候选
  6. 风险校验与最终决策

* 章节状态：

  * 已完成：绿色勾选

  * 生成中：蓝色动效

  * 等待输入：橙色提示

  * 排队中：灰色

  * 锁定：锁图标

**报告画布**

* 使用白色文档画布和浅灰工作区，视觉参考 Aime 右侧产物。

* 保留并重新组织现有高质量数据内容：

  * 顶部执行摘要。

  * V1/V15 总体四指标对比。

  * 重点人群、L4、L5 数据表。

  * 6,400 万 / 8,000 万规模条。

  * 预算覆盖测算。

  * 推荐候选与保量备选。

  * 风险和监控清单。

* 未生成章节显示骨架与依赖说明，不提前泄露最终结论。

* 点击队列章节滚动到报告对应锚点。

* 报告内容区独立滚动，中部对话位置不受影响。

### 6. 增加生成文件卡、右栏预览和下载

**文件**

* `/Users/bytedance/strategy2/index.html`

**生成时机**

* 用户完成风险确认并将最终建议记录为灰度候选后生成文件。

**中部文件卡**

* 文件名：`促中预算紧张_V1-V15策略分析报告.html`

* 展示：

  * HTML 图标

  * 文件大小

  * 生成时间

  * 6/6 章节完成

  * “查看报告”

  * “下载”

**右栏文件预览**

* 点击“查看报告”后，右栏从实时报告模式切换为文件预览模式。

* 使用 `iframe.srcdoc` 展示自包含报告，不依赖外部站点。

* 顶部工具栏提供：

  * 返回生成队列

  * 全屏/退出全屏

  * 下载 HTML

  * 关闭预览

* 全屏使用页面内覆盖层，不调用浏览器原生全屏权限，保证 Demo 稳定。

**下载实现**

* 从最终报告数据生成完整 HTML 字符串。

* 使用 `Blob` 与 `URL.createObjectURL()` 创建可下载文件。

* 新文件生成或退出案例时释放旧 Blob URL，避免内存泄漏。

### 7. 美化对话、队列与报告视觉

**文件**

* `/Users/bytedance/strategy2/index.html`

**视觉方向**

* 保留现有深色侧栏，避免仅重构一个案例时破坏全站品牌一致性。

* 案例主区域改为 Aime 式浅灰工作台：

  * 中栏白色。

  * 右栏浅灰背景 + 白色报告纸张。

  * 细边框和克制阴影。

* 继续使用：

  * 蓝色：主操作与报告生成状态。

  * 青绿色：AI 洞察、完成状态、正确结论。

  * 橙色：等待用户补充。

  * 红色：风险与止损。

* 消息样式按类型区分，不使用大量对称气泡：

  * 用户消息右对齐浅蓝卡。

  * AI 正文左对齐、宽内容块。

  * 分析过程使用紧凑时间线。

  * 操作建议使用按钮组。

  * 文件使用独立产物卡。

* 统一 8px 基础间距、10–12px 圆角和 12–14px 正文字号。

* 所有动画控制在 180–500ms，报告逐章生成使用轻量淡入与状态过渡。

* 支持 `prefers-reduced-motion`，减少动画用户可直接看到状态变化。

### 8. 保持既有能力与可访问性

**文件**

* `/Users/bytedance/strategy2/index.html`

**兼容要求**

* 原工作台以下功能保持不变：

  * 筛选

  * 图表指标切换

  * 竞对关注

  * 策略详情

  * 灰度候选

* 保留既有数据结论和正确因果：

  * 6.32 与 6.24 基本持平。

  * jp/t8 约 6,400 万，大盘 L4-L5 约 8,000 万。

  * V15 增量偏低主要是规模约束，不是效率显著更差。

  * 推荐大盘 L1-L5 部分人群算法覆盖。

  * jp/t8 保底 + 剩余预算算法分配为备选。

* 不调用真实发券、预算、人群或 AI 服务。

**可访问性**

* 三栏区域使用明确的 landmark 与 `aria-label`。

* 消息列表使用 `aria-live="polite"`，但生成过程合并播报，避免重复打扰。

* 队列章节使用有序列表和状态文本。

* Composer、报告切换、全屏、关闭、下载均有可读标签。

* 键盘支持：

  * Enter / Shift+Enter

  * Tab 顺序

  * Escape 退出全屏或关闭报告抽屉

  * 章节导航可键盘触发

* 全屏和抽屉关闭后焦点返回原触发按钮。

## Assumptions & Decisions

* 改造范围仅限“促中预算紧张”，不重构全部 Demo。

* 页面首次打开仍显示原促中调优工作台。

* 中部自由输入使用本地脚本化回答，不接真实 AI API。

* 进入案例时默认展示原始问题、AI 重述和待确认计划。

* 右侧为单份报告逐章生成，不建立多报告版本系统。

* 报告推进由中部对话动作驱动，不自动连续跑完，也不要求每章单独使用独立页面按钮。

* 最终文件在右栏内嵌预览，可全屏并实际下载 HTML。

* 原有 `budgetCaseData` 是报告和回答的唯一业务数据源，避免聊天与报告口径不一致。

* 继续使用单文件实现，不引入框架、构建系统或后端。

* 原 `.trae/specs/enrich-midcampaign-budget-dialog/` 规格文件不在本轮修改范围。

## Verification

### 静态检查

1. 提取所有内联 `<script>`，使用 Node `new Function()` 验证 JavaScript 语法。
2. 检查页面不存在重复 ID。
3. 检查所有按钮、输入框、队列章节、文件卡和预览操作具有可读名称。
4. 检查所有报告数字只来自 `budgetCaseData`，不存在第二份相互冲突的硬编码口径。

### 浏览器主链路

1. 首次打开仍显示原促中调优工作台。
2. 点击“促中预算紧张”进入三栏案例工作台。
3. 中栏显示用户问题、AI 重述、分析计划和固定输入框。
4. 右栏仅完成“问题与计划”，其余章节正确显示排队或锁定。
5. 确认计划后只推进总体章节，不自动跑完整报告。
6. 依次执行：

   * 继续人群下钻

   * 拆分规模效应

   * 输入预算或选择参数化分析

   * 确认风险

   * 生成候选
7. 每次动作同时更新：

   * 中部消息

   * 右侧队列状态

   * 右侧报告对应章节

   * 顶部完成进度
8. 输入自由问题，验证本地脚本化回答和四阶段过程。
9. 验证已有追问、加入计划、定位数据能力仍可工作。

### 文件与预览

1. 最终确认后出现 HTML 文件卡。
2. 点击“查看报告”在右栏打开 `iframe.srcdoc` 预览。
3. 预览可全屏、退出全屏、关闭并返回队列。
4. 下载得到可独立打开的 `.html` 文件，包含完整 6 章报告。
5. 多次生成不会累积失效 Blob URL。

### 回归与布局

1. 返回工作台后原筛选和图表状态不丢失。
2. 原工作台筛选、指标切换、竞对关注、详情弹窗、灰度候选全部可用。
3. 在约 1920px、1440px、1280px、1024px 宽度验证布局：

   * 无整页横向溢出。

   * 中部与右侧独立滚动。

   * 报告表格不遮挡。

   * 窄屏报告抽屉可关闭。
4. 检查控制台无新增错误。
5. 验证键盘输入、焦点恢复、Escape 和 reduced-motion。
